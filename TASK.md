# Current Task

## Goal
1차 리뷰 항목 4: 설정 페이지 추가

## Background
1차 리뷰 진행 순서:
1. 상단 예측 정보 구성 변경 ✅ 완료 (commit 4828b25)
2. 배란 예상일 마킹 변경 ✅ 완료 (commit 0fa3ebe)
3. 캘린더 기록 확인 방식 변경 + 사랑기록 신규 기능 ✅ 완료 (commit 398e063)
4. 설정 페이지 추가 ← 이번 작업

원 요청은 "알림 설정 / 로그인 및 계정 / 기타 앱 설정" 3개 섹션이었으나, 착수 전 확인 결과
사용자가 범위를 좁혀 **"알림 설정"과 "로그아웃" 딱 두 가지만** 넣기로 확정함.
"로그인 및 계정"의 파트너 연결 해제/계정 삭제 같은 파괴적 기능은 포함하지 않음.
"기타 앱 설정" 섹션 자체를 만들지 않기로 확정함.

## Scope
- 홈 화면 우측 상단에 설정(⚙️) 아이콘 추가, 탭하면 설정 페이지로 이동(뒤로가기로 복귀) — 3번 작업에서 만든
  날짜 상세 페이지와 동일한 "하위 화면 push/뒤로가기" 패턴 재사용
- 설정 페이지 구성: (1) 🔔 알림 설정 (2) 로그아웃 — 이 두 가지만
- 알림 설정: 기존에는 "켜기"만 가능했는데, 이번에 "끄기"(구독 해제) 기능을 새로 추가
- 홈 화면에 항상 떠 있던 알림 배너 / iOS 홈 화면 추가 안내 배너는 설정 페이지로 흡수되어 제거 (중복 표시 방지,
  1·2번 작업에서 유지해온 "캘린더와 중복되는 정보는 한 곳에만" 원칙과 동일선상)
- 계정 정보 표시, 초대 링크 관리 등은 이번 범위 아님 (홈 화면의 기존 초대 카드/파트너 카드는 그대로 유지)

## Definition of Done
- 홈 화면에서 설정 아이콘 탭 → 설정 페이지 진입, 뒤로가기로 복귀
- 설정 페이지에 "🔔 알림 설정"과 "로그아웃"만 존재
- 알림 미구독 상태: "알림 켜기" 버튼으로 구독 가능 (기존 로직 재사용)
- 알림 구독 상태: "알림 끄기" 버튼으로 구독 해제 가능 (신규) — 해제 후 새로고침해도 꺼진 상태 유지
- iOS 미설치 상태: 안내 문구 표시 (기존 배너 문구를 설정 페이지로 이전)
- 로그아웃 버튼 → 기존과 동일하게 로그아웃 동작
- 기존 기능(날짜 상세 페이지, 사랑기록, 캘린더, 예측 카드, 생리 기록 CRUD)에 회귀 없음

---

# Result

## Status
✅ Completed, push/배포 완료 (commit c8cd7e6, main으로 push)

## 문제 분석
알림 관련 UI가 홈 화면 상단에 항상 떠 있는 배너(구독 유도 또는 "켜져 있어요" 상태 표시)로만 존재했고,
한 번 켜면 끌 수 있는 방법이 UI에 없었음(백엔드 `DELETE /api/push/subscribe`는 이미 존재했지만 호출하는
프론트 코드가 없었음). "설정 페이지"라는 요청은 이런 제어를 홈 화면 상시 배너가 아니라 사용자가 원할 때
들어가서 확인/변경하는 전용 공간으로 옮기는 것으로 해석했고, 사용자와 협의해 범위를 "알림 설정 + 로그아웃"
두 가지로 확정함.

## 수정 내용
- `app.js`
  - `state.settingsOpen` 추가, `renderApp()`에서 `selectedDate` 다음으로 `settingsOpen` 분기 추가
  - 홈 화면 상단바의 "로그아웃" 텍스트 버튼 → "⚙️" 설정 아이콘 버튼(`data-action="open-settings"`)으로 교체
  - 홈 화면에서 `renderIOSBanner()`, `renderPushSection()` 호출 제거 (미사용 함수 자체도 삭제) — 알림 관련 안내는 설정 페이지로 일원화
  - 신규 `renderSettingsScreen()`: 뒤로가기 헤더(3번 작업의 `.detail-header` 재사용) + 알림 설정 카드 + 로그아웃 버튼
  - 신규 `renderNotificationSettingsCard(pushStatus)`: 구독됨/안됨/iOS 설치 필요/미지원 4가지 상태별 문구와 버튼 분기 (기존 `getPushStatus()` 로직 그대로 재사용)
  - 액션 핸들러 추가: `open-settings`, `close-settings`, `unsubscribe-push`
  - 신규 `handleUnsubscribePush()`: 브라우저 푸시 구독 해제(`subscription.unsubscribe()`) + 서버에 `DELETE /api/push/subscribe` 호출
- `style.css`
  - 안 쓰게 된 `.banner`, `.banner strong` 규칙 제거
  - `.icon-btn`을 이모지 아이콘 버튼(⚙️) 전용 스타일로 정리 (font-size 22px, 밑줄 hover 제거)
  - `.card p.hint` 추가 (설정 카드 안의 안내 문구용 — 기존 `.card`/muted 톤과 동일)

## 테스트 방법과 결과
1. `node --check app.js` 구문 검증 통과
2. 로컬 D1 + wrangler dev + Playwright(Chromium, 모바일 뷰포트)로 실제 화면 검증
   - 홈 화면 우측 상단에 ⚙️ 아이콘 정상 노출, 기존 배너들 사라져 화면이 정리됨
   - ⚙️ 탭 → "설정" 페이지 진입, "🔔 알림 설정"(미구독 상태 문구+"알림 켜기" 버튼) + "로그아웃" 버튼만 존재 확인
   - 뒤로가기(‹) → 홈 화면 정상 복귀
   - 설정 페이지에서 "로그아웃" → 로그인 화면으로 정상 전환 확인
3. 회귀 테스트: 설정 페이지 추가 후에도 3번 작업(날짜 탭 → 상세 페이지, 캘린더 기록/사랑기록 표시)이 정상 동작함을 재확인
4. 로컬 환경 특성상 실제 브라우저 푸시 구독은 헤드리스로 완전히 재현하기 어려워, "알림 끄기" 흐름은 코드 레벨로 확인:
   기존에 이미 검증된 `DELETE /api/push/subscribe` 엔드포인트(구독 삭제)를 그대로 호출하도록 연결했고,
   `subscribeToPush()`(기존, 변경 없음)와 대칭되는 구조로 작성함
5. 테스트 데이터 모두 정리, dev 서버 종료 완료

## 수정 파일
- `app.js`
- `style.css`

## 기존 기능에 미치는 영향
- 날짜 상세 페이지, 사랑기록, 캘린더 마킹, 예측 카드, 생리 기록 CRUD: 변경 없음, 회귀 테스트로 정상 확인
- 알림 관련 UI 위치만 이동(홈 화면 배너 → 설정 페이지), 구독/알림 발송 백엔드 로직은 변경하지 않음
- "로그아웃"을 상단바에서 바로 누르던 기존 동선이 사라지고, 이제는 ⚙️ → 설정 페이지 → 로그아웃 순서로 한 단계 늘어남

## 로컬 커밋 / 배포 여부
✅ 로컬 커밋 완료 (c8cd7e6), 사용자 확인 후 `origin/main`으로 push 완료.
Cloudflare Pages Git 연동을 통해 자동 배포됨. (새 DB 마이그레이션 없어 원격 D1 작업 불필요)

## Next Recommended Task
1차 리뷰 항목으로 요청받은 4개 작업(1~4)이 모두 완료됨. 추가로 진행할 항목이 있는지 확인 필요.
