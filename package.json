{
  "name": "product_02",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "둘만 쓰는 생리주기 예측 웹앱 (Cloudflare Pages + D1)",
  "scripts": {
    "dev": "wrangler pages dev . --compatibility-flag=nodejs_compat",
    "d1:migrate:local": "wrangler d1 execute period-tracker-db --local --file=./migrations/0001_init.sql",
    "d1:migrate:remote": "wrangler d1 execute period-tracker-db --remote --file=./migrations/0001_init.sql",
    "d1:migrate:local:love": "wrangler d1 execute period-tracker-db --local --file=./migrations/0002_love_logs.sql",
    "d1:migrate:remote:love": "wrangler d1 execute period-tracker-db --remote --file=./migrations/0002_love_logs.sql",
    "d1:migrate:local:notifications": "wrangler d1 execute period-tracker-db --local --file=./migrations/0003_notifications.sql",
    "d1:migrate:remote:notifications": "wrangler d1 execute period-tracker-db --remote --file=./migrations/0003_notifications.sql",
    "deploy": "wrangler pages deploy ."
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
